# Internal Application Configuration Section
server:
  port: ${APP_PORT:8080}

management:
  server:
    port: ${APP_PORT:8080}
    context-path: /api/v1
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: ["health", "info", "metrics", "env"]
  health:
    db:
      enabled: false

security:
  csrf:
    enabled: ${CSRF_ENABLED:false}
  cors:
    enable: ${CORS_ENABLED:false}

login-rate-limiting:
  locked-duration-ms: ${AUTHEN_LOGIN_RATE_LIMITING_LOCKED_DURARION_IN_MS:1800000} # default is 30 minutes
  attempt-window-ms: ${AUTHEN_LOGIN_RATE_LIMITING_ATTEMPT_WINDOW_IN_MS:900000} # default is 15 minutes
  ip-attempt-window-ms: ${AUTHEN_LOGIN_RATE_LIMITING_IP_ATTEMPT_WINDOW_IN_MS:60000} # default is 1 minute

# External System Configuration Section
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  # Database Configuration
  datasource:
    url: jdbc:postgresql://${POSTGRE_SQL_DB_HOST:localhost}:${POSTGRE_SQL_DB_PORT:5432}/${POSTGRE_SQL_DB_NAME:mydatabase}
    username: ${POSTGRE_SQL_DB_USERNAME:myuser}
    password: ${POSTGRE_SQL_DB_PASSWORD:mypassword}
    driver-class-name: org.postgresql.Driver
    hikari:
      maxLifetime: 30000
      idleTimeout: 10000
      connection-timeout: 2000
      validation-timeout: 1000

  # Redis Configuration
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 2000
    ssl: false

db:
  dbHostSource: ${POSTGRE_SQL_DB_HOST:localhost}
  dbPort: ${POSTGRE_SQL_DB_PORT:5432}
  dbName: ${POSTGRE_SQL_DB_NAME:mydatabase}

cache:
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    timeout: 2000
    database: 0
    ssl: false

# Secrets Section
app:
  jwt-secret: ${JWT_SECRET:DummySecretKeyWhichIsAtLeast32CharactersLong}
